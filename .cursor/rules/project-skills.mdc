---
description: 项目 Agent 技能索引与自动触发指引
alwaysApply: true
---

# 项目技能（自动加载）

技能源目录：`.agent/skills/`（经 `.cursor/skills/` 符号链接供 Cursor 发现）。

**规则**：用户任务匹配下表场景时，**必须先读取对应 `SKILL.md` 再执行**（可用 `@技能名` 显式触发）。

| 技能 | 触发场景 |
|------|----------|
| `scholar-search` | 文献搜索、下载 PDF、BibTeX、`references.bib` 核实、`verify_cited_papers.py` |
| `reference-processing` | 参考文献扩展、真实文献核验、正文引用同步、`data/papers/` 归档、文献处理报告 |
| `paper1-multi-agent-review` | 论文审核、投稿前审核、多角色评审、`reviews/` 报告 |
| `paper1-study-handbook` | 学习手册更新、`docs/study/` 同步 |
| `mygit` | git commit/push、智能提交、`mygit.sh` |
| `makeskill` | 新建/规范项目技能 |
| `gitnexus-cursor-mcp-setup` | 配置 GitNexus MCP、索引仓库 |

GitNexus 代码智能（`.claude/skills/gitnexus/`，经 `.agent/skills/gitnexus` → `.cursor/skills/gitnexus` 符号链接镜像）：改代码前跑 impact；探索用 query/context。

维护链接：`bash scripts/link_cursor_skills.sh`（含 gitnexus 包；analyze 更新后无需重跑，除非增删顶层技能项）
