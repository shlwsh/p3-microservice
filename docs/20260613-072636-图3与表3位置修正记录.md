# 图 3 与表 3 位置修正记录

时间：2026-06-13 07:26:36

## 问题

正文第 4.4 节引用图 3（指数退避重试延迟分布），但浮动体被 LaTeX 推迟到第 5 节之后显示；表 3（技术选型）属于第 5.1 节，但原文缺少就近引用句，且表格与 5.1 标题/正文关系不够清晰。

## 修正

1. 在 `latex/main-jos.tex` 加载 `float` 宏包。
2. 将 `latex/sections/zh/04_algorithms.tex` 中图 3 改为 `[H]`，并移动到首次引用句之后，确保图 3 属于第 4.4 节。
3. 在 `latex/sections/zh/05_implementation.tex` 第 5.1 节增加“表 3”就近引用句。
4. 将表 3 改为 `[H]`，固定在第 5.1 节引用句之后。

## 验证

- 编译命令：`bash scripts/build_pdf_jos.sh`
- 新版文稿：`docs/v51-论文稿件-jos-20260613-072636.pdf`
- PDF 页数：16 页。
- `.aux` 映射：`fig:backoff` 为图 3，第 7 页；`tab:tech` 为表 3，第 7 页。
- PDF 顺序：第 4.4 节引用图 3 后立即显示图 3；随后是上界说明；之后进入第 5 节；第 5.1 节引用表 3 后立即显示表 3。
- 日志检查：无 `Undefined`、无 `Overfull`、无 `Missing character`。
